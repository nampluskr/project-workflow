# INIT — 프로젝트 초기화 절차

> **이 문서는 에이전트가 읽고 그대로 실행한다.**
> 사람은 폴더에 문서를 두고 이 파일의 경로를 줄 뿐이다.

관련 문서 — 같은 레포 안에서 상대 경로로 참조한다.

| 필요할 때 | 문서 |
| --- | --- |
| 받은 문서를 검사할 때 | `DOC-SCHEMA.md` 9절 |
| 하네스를 설치할 때 | `HARNESS.md` |
| 파일 원본이 필요할 때 | `templates/` |

**필요한 것만 열어라.** 처음부터 전부 읽지 않는다.

---

## 0. 하지 않을 것

먼저 읽는다. 아래는 어떤 상황에서도 하지 않는다.

| 금지 | 이유 |
| --- | --- |
| 받은 5종을 고친다 | 요구가 사람 것이 아니게 된다 |
| 빠진 블럭을 대신 채운다 | 같음. **보고하고 멈춘다** |
| backlog CLI를 설치·재구현한다 | 프로젝트마다 다른 버전이 생긴다 |
| 이 가이드를 프로젝트로 복사한다 | 원본이 갱신돼도 옛 사본을 따른다 |
| `docs/history/` 아래 기존 파일을 수정·삭제한다 | 기록이 사후에 바뀐다 |
| `src/`·`tests/` 같은 빈 폴더를 미리 만든다 | 안 쓰는 폴더가 남고 구조가 굳는다 |
| 코드를 쓴다 | 초기화는 구조를 세우는 일이다. 구현은 그다음 |

---

## 1. 어느 경우인지 판별

| 상태 | 경우 |
| --- | --- |
| `docs/current/`가 없다 | **A — 신규 프로젝트** (`v0.1`) |
| `docs/current/`가 있다 | **B — 새 버전** (`v0.2`+) |

판별한 결과를 먼저 말하고 시작한다.

---

## 2. 전제 확인 — 공통

모두 확인한다. **하나라도 걸리면 멈추고 보고한다.**

| # | 확인 | 실패 시 |
| --- | --- | --- |
| 1 | backlog CLI가 실행된다 | **멈춤.** 설치하지 말고 보고 |
| 2 | 루트에 5종이 있다 | **멈춤.** 무엇이 없는지 보고 |
| 3 | (A일 때만) 루트에 `README.md`가 있다 | **멈춤.** 보고 |
| 4 | 프로젝트 폴더명이 단어를 `-`로 연결한 `project-name` 형식이며 `_`를 포함하지 않는다 | **멈춤.** 폴더명 변경을 요청 |
| 5 | GitHub `origin`이 있으면 저장소명이 프로젝트 폴더명과 정확히 같다 | **멈춤.** 저장소명 또는 연결 수정 요청 |

---

## 3. 문서 검사

`DOC-SCHEMA.md` 9절의 검사 목록을 그대로 돌린다.

**결과를 표로 보고한다.** 통과 항목도 적는다. 무엇을 확인했는지 사람이 알아야 한다.

```
| 검사 | 결과 |
| --- | --- |
| 5종 존재 | 통과 |
| 버전 표기 일치 (v0.1) | 통과 |
| BRIEF 필수 블럭 5개 | 4번 "하지 않을 것" 없음 ← 실패
...
```

**하나라도 실패하면 여기서 멈춘다.** 다음 단계로 가지 않는다.

---

## 4. A — 신규 프로젝트 (`v0.1`)

| # | 할 것 | 종료 조건 |
| --- | --- | --- |
| 1 | `docs/current/` 생성 | 폴더 존재 |
| 2 | 루트의 5종을 `docs/current/`로 **이동** | 루트에서 사라지고 `current/`에 있음 |
| 3 | `docs/current/PROGRESS.md` 생성 | `DOC-SCHEMA.md` 7절 두 블럭이 빈 채로 있음 |
| 4 | `.gitignore` 생성 | `templates/`에서 언어·런타임에 맞는 것 |
| 5 | `git init` | — |
| 6 | 기준 커밋 | `git status --short`가 비어 있음 |
| 7 | 하네스 설치 | 6절 |
| 8 | 차단 증명 | 7절 |
| 9 | 보고 | 8절 |

`docs/history/`는 만들지 않는다. **첫 마감 때 생긴다.**
`src/`·`scripts/`·`tests/`도 만들지 않는다.
GitHub 저장소를 만들거나 연결할 때는 프로젝트 폴더명과 정확히 같은 이름을 사용한다.

---

## 5. B — 새 버전 (`v0.2`+)

**먼저 확인한다.**

| # | 확인 | 실패 시 |
| --- | --- | --- |
| 1 | 직전 버전의 `docs/history/vX.Y/`가 있다 | **멈춤.** 이전 버전이 마감되지 않았다 |
| 2 | 그 폴더에 6종이 다 있다 | **멈춤.** 마감이 불완전하다 |
| 3 | 루트의 새 5종 버전 표기가 직전보다 높다 | **멈춤.** 버전 번호를 확인받는다 |

1번이 실패하면 `docs/current/`를 건드리지 않는다. 아직 기록되지 않은 버전이
거기 있을 수 있다.

**그다음 진행한다.**

| # | 할 것 | 종료 조건 |
| --- | --- | --- |
| 4 | `docs/current/`의 기존 파일 제거 | 비어 있음 |
| 5 | 루트의 새 5종을 `docs/current/`로 이동 | 루트에서 사라짐 |
| 6 | 새 `PROGRESS.md` 생성 | 빈 두 블럭 |
| 7 | **이전 `BRIEF` 대조** | 아래 |
| 8 | 하네스 점검 | 6절. 바뀐 게 없으면 그대로 둔다 |
| 9 | 보고 | 8절 |

### 7번 — 이전 BRIEF 대조

`docs/history/vX.Y/BRIEF.md`와 `docs/current/BRIEF.md`를 읽고, **무엇이 달라졌는지
요약해서 사람에게 확인받는다.**

읽는 순서가 있다.

1. 이번 `BRIEF`의 **"이전 버전에서 아쉬웠던 것"** — 사람이 직접 쓴 것이다. 이게 기준
2. 이전 `PROGRESS.md`의 **"계획 외 개선"** — 지난 버전에서 계획 밖으로 붙은 것
3. 두 `BRIEF`의 "이번 버전에서 풀고 싶은 문제" 비교

**요약 형식**

```
이전 버전 (v0.1) — 무엇을 하려 했나
이번 버전 (v0.2) — 무엇을 하려 하나
달라진 점 — 세 줄 이내
지난 버전 계획 외 개선 중 이번에 반영된 것 / 안 된 것
```

**사람이 승인할 때까지 다음으로 가지 않는다.** 요구를 잘못 읽은 채 Phase를 다 돌면
그때는 되돌리기 어렵다.

---

## 6. 하네스 설치

절차와 내용은 `HARNESS.md`를 따른다. 여기서는 결과만 정한다.

| 대상 | 상태 |
| --- | --- |
| `CLAUDE.md` | 루트에 있고, 첫 줄에 가이드 버전이 적혀 있다 |
| `.claude/rules/` | 경로별 금지 규칙 |
| `.claude/hooks/` | `PreToolUse` — history·backlog 직접 편집 차단 |
| `.claude/agents/` | 리뷰어 |
| `.claude/settings.json` | hook 등록 |

`CLAUDE.md` 첫 줄:

```
project-workflow v1.0 기준으로 초기화됨 (2026-09-01)
```

가이드가 갱신되면 예전에 초기화한 프로젝트와 구조가 달라진다. 기록이 없으면
나중에 왜 다른지 알 수 없다.

**B의 경우** — 하네스는 프로젝트 층이므로 이미 있다. 새로 깔지 않는다.
`DECISIONS.md`에서 `CLAUDE.md`·`rules`로 승격된 항목이 반영됐는지만 확인한다.

---

## 7. 차단 증명 — A에서만

**등록된 것과 동작하는 것은 다르다.** 경로 오타나 종료 코드 문제로 조용히
무력화되는 일이 흔하다.

가짜 입력으로 실제 막히는지 확인한다.

| 시도 | 기대 |
| --- | --- |
| `docs/history/` 아래 파일 수정 | 차단됨 |
| `backlog.json` 직접 편집 | 차단됨 |

**막히지 않으면 초기화 실패다.** 보고하고 멈춘다. 등록만 하고 넘어가면 있으나
마나 한 장치가 된다.

---

## 8. 보고

사람이 승인할 재료다. 이 항목을 빠짐없이 적는다.

```
경우          A(신규) 또는 B(새 버전) / 버전 번호
문서 검사     항목별 통과·실패 표
만든 것       폴더와 파일 목록
git           커밋 해시, 작업 트리 상태
하네스        설치·점검 결과
차단 증명     시도와 결과 (A만)
BRIEF 대조    요약 (B만)
다음          "Phase 1부터 진행하시겠습니까"
```

**승인 없이 구현으로 넘어가지 않는다.**

---

## 9. 멈춤 규칙

아래 상황에서는 **판단하지 말고 멈춘다.** 사람에게 무엇이 문제인지 말하고 기다린다.

- backlog CLI가 없다
- 5종 중 하나라도 없다
- 문서 검사에 실패한 항목이 있다
- 버전 표기가 서로 다르거나 직전 버전보다 낮다
- 직전 버전이 마감되지 않았다 (B)
- 차단 증명이 실패했다
- 문서 내용이 서로 모순된다

**마지막 항목이 중요하다.** `SPEC`에 있는 요구가 `PLAN`의 어느 Phase에도 없거나,
`backlog`의 task가 `PLAN`에 없는 Phase를 가리키는 경우다. 채워서 맞추지 말고
어긋난 곳을 보고한다.

---

## 10. 버전 이력

| 버전 | 변경 |
| --- | --- |
| 1.0 | 최초 작성 |
