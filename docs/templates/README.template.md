# <프로젝트 이름>

> 개요는 `v0.1` 착수 전에 **사람이** 채운다 (에이전트가 `INTENT.md`를 참고해 초안을 도울 수 있다).
> 설치·사용법·요구 환경은 버전 마감 때 갱신한다. 착수 시점에 쓰지 않는다.
> 이 안내 줄은 지운다.

## 개요

<!--
한두 문단. 이 프로젝트가 무엇을 하는 도구인지 밖에서 처음 보는 사람에게 소개한다.

판정 기준(이 프로젝트가 무엇을 왜 하는가)은 여기가 아니라 docs/current/INTENT.md에 있다.
개요는 그 INTENT를 밖으로 풀어 쓴 소개이며, INTENT와 어긋나면 안 된다.

기능을 나열하기보다 무엇을 하는 도구인지 한눈에 들어오게 적는다.
-->

## 설치

<!--
버전 마감 때 갱신한다. 실제로 되는 것만 적는다.
-->

## 사용법

<!--
버전 마감 때 갱신한다. 실제로 동작하는 것만 적는다.
SPEC에 있었지만 안 된 것은 적지 않는다.

실행 예 · 주요 옵션
-->

## 요구 환경

<!-- 런타임, 의존 도구. 버전 고정이 있으면 명시 -->

---

이 프로젝트가 무엇을 왜 하는가(SSOT)는 `docs/current/INTENT.md`에 있다.
현재 버전 문서는 `docs/current/`, 지난 버전은 `docs/history/`에 있다.
